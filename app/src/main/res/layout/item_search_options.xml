<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="model"
            type="com.example.liflow.presentation.ui.search.model.SearchOptions" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/item_search_options_constraintLayout_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/item_search_options_imageView_picture"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:scaleType="fitXY"
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/text_size_caption"
            android:tint="@color/colorWhite"
            android:padding="20dp"
            app:imageViewBindingAdapter="@{model.drawableId}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.49"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_chainStyle="packed" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>